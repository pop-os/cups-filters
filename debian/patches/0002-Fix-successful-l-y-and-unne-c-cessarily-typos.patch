From: Didier Raboud <odyx@debian.org>
Date: Tue, 25 Aug 2020 22:09:08 +0200
Subject: Fix successful{,l}y and unne{c,}cessarily typos

---
 utils/cups-browsed.conf.5  | 4 ++--
 utils/cups-browsed.conf.in | 4 ++--
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/utils/cups-browsed.conf.5 b/utils/cups-browsed.conf.5
index 0b46805..3f2ff80 100644
--- a/utils/cups-browsed.conf.5
+++ b/utils/cups-browsed.conf.5
@@ -423,7 +423,7 @@ or "Off" lets cups-browsed not use CUPS' domain socket.
 .fi
 Set HTTP timeout (in seconds) for requests sent to local/remote
 resources Note that too short timeouts can make services getting
-missed when they are present and operations be unneccessarily
+missed when they are present and operations be unnecessarily
 repeated and too long timeouts can make operations take too long
 when the server does not respond.
 .PP
@@ -436,7 +436,7 @@ when the server does not respond.
 .fi
 Set how many retries (N) should cups-browsed do for creating print
 queues for remote printers which receive timeouts during print queue
-creation.  The printers which are not successfuly set up even after N
+creation.  The printers which are not successfully set up even after N
 retries, are skipped until the next restart of the service.  Note that
 too many retries can cause high CPU load.
 .PP
diff --git a/utils/cups-browsed.conf.in b/utils/cups-browsed.conf.in
index 898fe24..e89d325 100644
--- a/utils/cups-browsed.conf.in
+++ b/utils/cups-browsed.conf.in
@@ -308,7 +308,7 @@ BrowseRemoteProtocols @BROWSEREMOTEPROTOCOLS@
 
 # Set HTTP timeout (in seconds) for requests sent to local/remote
 # resources Note that too short timeouts can make services getting
-# missed when they are present and operations be unneccessarily
+# missed when they are present and operations be unnecessarily
 # repeated and too long timeouts can make operations take too long
 # when the server does not respond.
 
@@ -317,7 +317,7 @@ BrowseRemoteProtocols @BROWSEREMOTEPROTOCOLS@
 
 # Set how many retries (N) should cups-browsed do for creating print
 # queues for remote printers which receive timeouts during print queue
-# creation.  The printers which are not successfuly set up even after
+# creation.  The printers which are not successfully set up even after
 # N retries, are skipped until the next restart of the service.  Note
 # that too many retries can cause high CPU load.
 
